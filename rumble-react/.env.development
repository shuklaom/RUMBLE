# Suppress warning messages about vulnerabilities during development
GENERATE_SOURCEMAP=false

# Disable browser opening automatically on start
BROWSER=none

# Skip security warnings since they're known and acknowledged
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Increase Node.js memory limit if needed for larger builds
NODE_OPTIONS=--max-old-space-size=4096
